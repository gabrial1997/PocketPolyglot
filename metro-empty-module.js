// Intentionally empty. Used by metro.config.js to stub out optional packages
// that some dependencies import but that we do not ship (e.g. @opentelemetry/api,
// referenced by @supabase/supabase-js for optional tracing). See metro.config.js.
module.exports = {};
