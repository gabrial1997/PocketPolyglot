# Dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
*.tsbuildinfo

# Native
ios/
android/
*.orig.*

# Env — never commit real keys
.env
.env.local
.env.*.local

# Generated TTS audio (content pipeline)
content-pipeline/out/
content-pipeline/voice-test/

# Logs / OS
npm-debug.*
yarn-error.*
.DS_Store

# Ephemeral live-session scratch (tunnel URL + PIDs — never commit)
LIVE_SESSION.md
.superpowers/
